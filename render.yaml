services:
  - type: web
    name: routegenius-app
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: routegenius-db
          property: connectionString
      - key: SPRING_PROFILES_ACTIVE
        value: production
      - key: MAIL_USERNAME
        value: teamroutemax@gmail.com
      - key: MAIL_PASSWORD
        value: cbfn tlwn keil eimk

databases:
  - name: routegenius-db
    databaseName: routegenius
    user: geniususer
